name: CI

on:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    name: Tests
    runs-on: macos-26

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Run tests
        run: script/test.sh

